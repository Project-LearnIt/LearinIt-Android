<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_logo"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="40dp"
        android:layout_centerInParent="true"/>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="35dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true">

        <TextView
            android:id="@+id/loading_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/hey_pretty_girl"
            android:text="@string/loading_title"
            android:textAllCaps="true"
            android:textColor="@color/colorLightText"
            android:textSize="25sp"/>

        <com.eyalbira.loadingdots.LoadingDots
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/loading_textview"
            android:layout_toEndOf="@+id/loading_textview"
            android:layout_marginBottom="6dp"
            android:layout_marginStart="4dp"
            app:LoadingDots_dots_color="@color/colorLightText"
            app:LoadingDots_dots_count="3"
            app:LoadingDots_dots_size="5dp"
            app:LoadingDots_dots_space="2dp"/>

    </RelativeLayout>

</RelativeLayout>